import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'miAppMovil-2070801',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
